name: Final Kernel Solution

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout and fix all issues
      run: |
        echo "🔧 开始修复所有问题..."
        
        # 手动克隆仓库
        git clone https://github.com/liu96572/InfiniR_kernel_alioth.git src
        cd src
        
        echo "✅ 代码克隆完成"
        
        # 修复脚本权限
        echo "修复脚本权限..."
        find scripts -name "*.sh" -exec chmod +x {} \; 2>/dev/null || echo "部分权限修复完成"
        
        # 处理KernelSU配置
        echo "处理KernelSU配置..."
        if [ -d "drivers/kernelsu" ]; then
          echo "KernelSU目录已存在，检查Kconfig..."
          if [ ! -f "drivers/kernelsu/Kconfig" ]; then
            echo "创建KernelSU Kconfig文件..."
            echo 'config KSU' > drivers/kernelsu/Kconfig
            echo '    bool "KernelSU"' >> drivers/kernelsu/Kconfig
            echo '    default n' >> drivers/kernelsu/Kconfig
            echo '    help' >> drivers/kernelsu/Kconfig
            echo '      KernelSU support' >> drivers/kernelsu/Kconfig
          else
            echo "KernelSU Kconfig已存在"
          fi
        else
          echo "创建KernelSU目录和Kconfig..."
          mkdir -p drivers/kernelsu
          echo 'config KSU' > drivers/kernelsu/Kconfig
          echo '    bool "KernelSU"' >> drivers/kernelsu/Kconfig
          echo '    default n' >> drivers/kernelsu/Kconfig
        fi
        
        # 修复VDSO问题 - 直接修改signal.c避免VDSO编译错误
        echo "修复VDSO编译问题..."
        if [ -f "arch/arm64/kernel/signal.c" ]; then
          # 备份原文件
          cp arch/arm64/kernel/signal.c arch/arm64/kernel/signal.c.backup
          
          # 注释掉VDSO相关代码
          sed -i 's/sigtramp = VDSO_SYMBOL(current->mm->context.vdso, sigtramp);/\/\* sigtramp = VDSO_SYMBOL(current->mm->context.vdso, sigtramp); VDSO disabled \*\//g' arch/arm64/kernel/signal.c
          sed -i 's/sigtramp = VDSO_SYMBOL(current->mm->context.vdso, rt_sigtramp);/\/\* sigtramp = VDSO_SYMBOL(current->mm->context.vdso, rt_sigtramp); VDSO disabled \*\//g' arch/arm64/kernel/signal.c
          
          echo "✅ VDSO代码已注释"
        fi
        
        echo "🎯 所有修复完成"
        cd ..

    - name: Setup build environment
      run: |
        echo "📦 安装编译环境..."
        sudo apt-get update
        sudo apt-get install -y build-essential libncurses-dev libssl-dev bc flex bison libelf-dev
        sudo apt-get install -y gcc-aarch64-linux-gnu g++-aarch64-linux-gnu
        
        echo "✅ 环境准备完成"

    - name: Build kernel
      run: |
        cd src
        export ARCH=arm64
        export SUBARCH=arm64
        export CROSS_COMPILE=aarch64-linux-gnu-
        
        echo "⚙️ 开始内核配置..."
        
        # 创建输出目录
        mkdir -p out
        
        # 复制默认配置
        cp arch/arm64/configs/alioth_defconfig out/.config
        
        # 添加必要配置（禁用VDSO）
        {
          echo "# 基本功能"
          echo "CONFIG_ZSTD_COMPRESS=y"
          echo "CONFIG_KERNEL_ZSTD=y"
          echo "CONFIG_ZRAM=y"
          echo "CONFIG_SWAP=y"
          echo "CONFIG_CGROUP_FREEZER=y"
          echo "CONFIG_DEBUG_FS=y"
          
          echo "# 禁用VDSO避免编译错误"
          echo "CONFIG_VDSO=n"
          echo "CONFIG_ARM64_VDSO=n"
          
          echo "# 其他配置"
          echo "CONFIG_MODULES=y"
          echo "CONFIG_KSU=n"
        } >> out/.config
        
        # 应用配置
        make O=out ARCH=arm64 olddefconfig
        echo "✅ 内核配置完成"
        
        echo "🔨 开始编译内核..."
        make O=out ARCH=arm64 -j$(nproc)
        
        if [ -f "out/arch/arm64/boot/Image.gz-dtb" ]; then
          echo "🎉 内核构建成功！"
          echo "📊 内核文件信息:"
          ls -lh out/arch/arm64/boot/Image.gz-dtb
          file out/arch/arm64/boot/Image.gz-dtb
        else
          echo "❌ 构建失败"
          echo "检查错误日志..."
          find out -name "*.o" -type f | head -5
          exit 1
        fi

    - name: Create flashable package
      run: |
        cd src
        echo "📦 创建刷机包..."
        
        git clone https://github.com/osm0sis/AnyKernel3
        cp out/arch/arm64/boot/Image.gz-dtb AnyKernel3/
        
        # 创建刷机脚本
        cat > AnyKernel3/anykernel.sh << 'EOF'
        #!/system/bin/sh
        # AnyKernel3 Ramdisk Mod Script
        # 最终解决方案内核
        
        ## AnyKernel setup
        properties() { '
        kernel.string=Final Solution Kernel
        do.devicecheck=1
        do.modules=0
        do.systemless=1
        do.cleanup=1
        do.cleanuponabort=0
        device.name1=alioth
        device.name2=aliothin
        '; }
        
        ## AnyKernel methods
        . tools/ak3-core.sh;
        
        ## AnyKernel install
        dump_boot;
        write_boot;
        EOF
        
        cd AnyKernel3
        zip -r9 ../../final_kernel.zip . -x "*.git*"
        cd ../..
        
        echo "✅ 刷机包创建完成"
        ls -lh final_kernel.zip

    - name: Upload kernel
      uses: actions/upload-artifact@v4
      with:
        name: final-solution-kernel
        path: final_kernel.zip
        retention-days: 30
